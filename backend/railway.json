{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "python -m app.deploy_check && alembic upgrade head",
    "startCommand": "python -m app.deploy_check && python -m app.schema_check --wait-seconds 120 && exec uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-3001} --no-access-log",
    "healthcheckPath": "/ready",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
