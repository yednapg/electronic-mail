{
  "name": "electronic-mail-system",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "bash scripts/bootstrap.sh",
    "start": "npm run setup && npm run dev",
    "dev": "concurrently \"npm run dev -w @electronic-mail/web\" \"npm run dev -w @electronic-mail/api\" \".venv/bin/python -m uvicorn main:app --reload --app-dir apps/ai --port 8001\"",
    "dev:web": "npm run dev -w @electronic-mail/web",
    "dev:api": "npm run dev -w @electronic-mail/api",
    "dev:ai": ".venv/bin/python -m uvicorn main:app --reload --app-dir apps/ai --port 8001",
    "db:generate": "npm run db:generate -w @electronic-mail/api",
    "db:push": "npm run db:push -w @electronic-mail/api",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write .",
    "typecheck": "npm run typecheck -w @electronic-mail/types && npm run typecheck -w @electronic-mail/web && npm run typecheck -w @electronic-mail/api"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.28.0",
    "@typescript-eslint/parser": "^8.28.0",
    "concurrently": "^9.1.2",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^10.1.1",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3"
  }
}
